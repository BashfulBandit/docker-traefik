#!/usr/bin/env bash

# Determine if use wants to run Docker Swarm Mode or Docker Containers.
# Write to a file that this is Swarm Mode or Container environment.

# Determine if user has a .env file.
# If not, then prompt user for information needed in the .env file.
# Allow user to quit at anytime to create their .env file manually.

# Create acme.json file and log files(access.log, error.log).

# Start command based on if Docker Swarm or Docker Containers.
